//Para rodar o programa:

// 1- Tenha o Node instalado;
// 2- Consulte a versão do Node no terminal digitando "node -v";
// 3- No terminal: cd 'caminho da pasta que deseja acessar';
// 4- Feito isso, precisamos rodar o programa, digite node 'inicio do nome do arquivo que deseja rodar' + TAB;
